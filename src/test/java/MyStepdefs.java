public class MyStepdefs {
    public MyStepdefs() {
        Given("^Two input values, (\\d+) and (\\d+)$", (Integer arg0, Integer arg1) -> {
        });
        When("^I calculate sqrt\\(a/b\\)$", () -> {
        });
        Then("^I expect the result (\\d+)\\.(\\d+)$", (Integer arg0, Integer arg1) -> {
        });
    }
}
